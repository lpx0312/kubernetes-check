---

### 修订版 Kubernetes Pod 状态监控程序需求文档（适配 Kubernetes 1.17.2）

---

#### **1. 功能需求**  
##### **1.1 核心功能**  
1. **实时获取所有Pod状态**  
   - 输出所有Pod的以下字段：  
     `Namespace | PodName | PodStatus | Node | RestartCount | LastRestartTime | RestartReason | ReadyStatus`  
   - **字段调整说明**：  
     - `RestartCount`: 仅统计容器最后一次重启事件（Kubernetes 1.17 仅暴露最后一次终止状态，无法获取完整历史重启次数）。  
     - `LastRestartTime`: 取自最后一次容器终止时间（若存在）。  

2. **7天内重启的Pod筛选**  
   - 若 `LastRestartTime` 在7天内，则判定为需展示的Pod。  
   - 重启原因取自最后一次容器终止的 `Reason` 字段。  

3. **表格化输出**  
   - 对齐列宽，支持命令行友好阅读（与初始需求一致）。  

---

#### **2. 技术需求**  
##### **2.1 输入与依赖**  
- **Kubernetes版本**: 1.17.2  
- **依赖库**:  
  - Go 1.13.9（Kubernetes 1.17.2官方明确要求版本，需严格匹配） client-go必须锁定v0.17.x版本（新版client-go不兼容1.17 API）
  - 表格格式化库 `github.com/olekukonko/tablewriter`（无版本限制）。  

##### **2.2 关键代码逻辑调整**  
1. **API调用方式变更**  
   - **List方法参数**：旧版本 `client-go` 使用 `metav1.ListOptions{}` 而非 `context` 参数。  
     ```go
     pods, err := clientset.CoreV1().Pods("").List(metav1.ListOptions{})
     ```  

2. **重启次数与时间处理**  
   - **单容器Pod**：直接读取 `ContainerStatuses[0].LastTerminationState.Terminated`。  
   - **多容器Pod**：遍历所有容器，取最新的 `FinishedAt` 时间作为 `LastRestartTime`。  

3. **节点IP获取**  
   - 需显式调用 `clientset.CoreV1().Nodes().Get(pod.Spec.NodeName)` 查询Node对象。  

---

#### **3. 版本差异限制**  
- **历史重启次数不可追溯**：Kubernetes 1.17 仅记录最后一次容器终止状态，无法获取7天内所有重启事件次数。  
  - 需求调整：`RestartCount` 字段实际含义为"是否在7天内重启过"（0或1）。  
- **Termination状态字段兼容性**：确保 `Terminated.FinishedAt` 和 `Terminated.Reason` 字段在1.17.2中存在。  

---

#### **4. RBAC权限配置**  
```yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: pod-monitor-reader
rules:
- apiGroups: [""]
  resources: ["pods", "nodes"]
  verbs: ["get", "list"]
```

---

#### **5. 非功能需求更新**  
1. **兼容性**  
   - 明确支持 Kubernetes 1.17.x，不保证在更高版本（≥1.22）运行。  
2. **日志输出**  
   - 添加 `--version` 参数输出版本兼容性信息。  

---

#### **6. 输出示例（调整说明）**  
```plaintext
Namespace            PodName                                  PodStatus  Node           Restart  RestartTime               RestartReason  READY
datacenter-prod      trino-worker-6d7c9ddb6b-52lxn            Running    10.18.130.226  1        2025-02-16 09:44:54 UTC   OOMKilled      1/1
```  
- `Restart` 列实际表示"是否在7天内重启过"（1=是，0=否）。  

---

#### **7. 注意事项**  
1. **旧版本限制**：  
   - 无法获取容器历史重启次数，仅能判断最后一次重启是否在7天内。  
   - `CrashLoopBackOff` 状态需从 `Pod.Status.Reason` 字段提取，而非容器状态。  
2. **时区处理**：所有时间字段统一为UTC格式。  

---

#### **8. 设计伪代码调整**  
```go
// 关键逻辑修改点
func analyzeContainers(statuses []v1.ContainerStatus) (int, time.Time, string) {
    // 添加空状态检查
    if len(statuses) == 0 {
        return 0, time.Time{}, ""
    }
    
    // 变量名优化
    var latestRestartTime time.Time
    restartReason := ""
    
    // 增加状态类型检查
    for _, cs := range statuses {
        if cs.LastTerminationState.Terminated != nil {
            terminated := cs.LastTerminationState.Terminated
            // 添加时间有效性校验
            if terminated.FinishedAt.IsZero() {
                continue
            }
            if terminated.FinishedAt.After(latestRestartTime) {
                latestRestartTime = terminated.FinishedAt.Time
                restartReason = terminated.Reason
            }
        }
    }
    
    // 增加时区显式转换
    cutoffTime := time.Now().UTC().Add(-7 * 24 * time.Hour)
    if latestRestartTime.After(cutoffTime) {
        return 1, latestRestartTime.UTC(), restartReason // 显式返回UTC时间
    }
    return 0, time.Time{}, ""
}
```
